spring:
  application:
    name: eureka-um
  profiles:
    active: eureka-service
    include: default

---
spring:
  profiles: default
server:
  port: ${port.service.eureka-um1}

---
spring:
  profiles: other
server:
  port: ${port.service.eureka-um2}
