spring:
  application:
    name: eureka-um
  profiles:
    include: default
    active: eureka-service

---
spring:
  profiles: default
server:
  port: ${port.service.eureka-um1}

---
spring:
  profiles: other
server:
  port: ${port.service.eureka-um2}
