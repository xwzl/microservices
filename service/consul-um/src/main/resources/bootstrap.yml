spring:
  application:
    name: consul-um
#  cloud:
#    consul:
#      host: ${ip.home}
#      port: 8500
#      discovery:
#        service-name: ${spring.application.name}

server:
  port: ${port.service.${spring.application.name}}