spring:
  application:
    name: consul-customer-service
#  cloud:
#    consul:
#      host: ${ip.home}
#      port: 8500
#      discovery:
#        service-name: ${spring.application.name}

server:
  port: ${port.api.${spring.application.name}}
